// Generated by valentine_gen. Do not edit.

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unused_parens)]
#![allow(clippy::all)]
use ::bitflags::bitflags;
use bytes::{Buf, BufMut};
use super::*;
use super::super::types::*;
use crate::bedrock::codec::BedrockCodec;
pub use crate::bedrock::protocol::v1_21_42::PacketPlayerAction as PacketPlayerAction;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerHotbar as PacketPlayerHotbar;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerInput as PacketPlayerInput;
pub use crate::bedrock::protocol::v1_20_61::PacketPlayerList as PacketPlayerList;
pub use crate::bedrock::protocol::v1_19_62::PacketPlayerSkin as PacketPlayerSkin;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInput as PacketPlayerAuthInput;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputArgs as PacketPlayerAuthInputArgs;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputBlockActionItem as PacketPlayerAuthInputBlockActionItem;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputBlockActionItemContent as PacketPlayerAuthInputBlockActionItemContent;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputBlockActionItemContentAbortBreak as PacketPlayerAuthInputBlockActionItemContentAbortBreak;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputContent as PacketPlayerAuthInputContent;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputInputMode as PacketPlayerAuthInputInputMode;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputInteractionModel as PacketPlayerAuthInputInteractionModel;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputPlayMode as PacketPlayerAuthInputPlayMode;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputTransaction as PacketPlayerAuthInputTransaction;
pub use crate::bedrock::protocol::v1_21_60::PacketPlayerAuthInputTransactionArgs as PacketPlayerAuthInputTransactionArgs;
pub use crate::bedrock::protocol::v1_16_220::PacketPlayerEnchantOptions as PacketPlayerEnchantOptions;
pub use crate::bedrock::protocol::v1_21_20::PacketPlayerArmorDamage as PacketPlayerArmorDamage;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerFog as PacketPlayerFog;
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum PacketPlayerVideoCaptureAction {
    Stop = 0,
    Start = 1,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerVideoCaptureAction {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as u8;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketPlayerVideoCaptureAction::Stop),
            1 => Ok(PacketPlayerVideoCaptureAction::Start),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketPlayerVideoCaptureAction), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketPlayerVideoCaptureContent {
    pub frame_rate: i32,
    pub file_prefix: String,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerVideoCaptureContent {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::I32LE(self.frame_rate).encode(buf)?;
        let bytes: Vec<u8> = if "".eq_ignore_ascii_case("latin1") {
            (&self.file_prefix)
                .chars()
                .map(|ch| {
                    let code = ch as u32;
                    if code <= 0xFF { code as u8 } else { b'?' }
                })
                .collect()
        } else {
            (&self.file_prefix).as_bytes().to_vec()
        };
        let len = bytes.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        buf.put_slice(&bytes);
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let frame_rate = <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let file_prefix = {
            let len_raw = (<crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as i64;
            if len_raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "string length cannot be negative",
                    ),
                );
            }
            let len = len_raw as usize;
            if buf.remaining() < len {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::UnexpectedEof,
                        format!(
                            "string declared length {} exceeds remaining {}", len, buf
                            .remaining()
                        ),
                    ),
                );
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            let s = if "".eq_ignore_ascii_case("latin1") {
                bytes.into_iter().map(|b| b as char).collect::<String>()
            } else {
                String::from_utf8_lossy(&bytes).into_owned()
            };
            s
        };
        Ok(Self { frame_rate, file_prefix })
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketPlayerVideoCapture {
    pub action: PacketPlayerVideoCaptureAction,
    pub content: Option<PacketPlayerVideoCaptureContent>,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerVideoCapture {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.action.encode(buf)?;
        if let Some(v) = &self.content {
            v.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let action = <PacketPlayerVideoCaptureAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let content = match action {
            PacketPlayerVideoCaptureAction::Start => {
                Some(
                    <PacketPlayerVideoCaptureContent as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?,
                )
            }
            _ => None,
        };
        Ok(Self { action, content })
    }
}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum PacketPlayerUpdateEntityOverridesType {
    ClearAll = 0,
    Remove = 1,
    SetInt = 2,
    SetFloat = 3,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerUpdateEntityOverridesType {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as u8;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketPlayerUpdateEntityOverridesType::ClearAll),
            1 => Ok(PacketPlayerUpdateEntityOverridesType::Remove),
            2 => Ok(PacketPlayerUpdateEntityOverridesType::SetInt),
            3 => Ok(PacketPlayerUpdateEntityOverridesType::SetFloat),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketPlayerUpdateEntityOverridesType), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, PartialEq)]
pub enum PacketPlayerUpdateEntityOverridesValue {
    SetFloat(f32),
    SetInt(i32),
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketPlayerUpdateEntityOverrides {
    pub runtime_id: i64,
    pub property_index: i32,
    pub type_: PacketPlayerUpdateEntityOverridesType,
    pub value: Option<PacketPlayerUpdateEntityOverridesValue>,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerUpdateEntityOverrides {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::VarLong(self.runtime_id).encode(buf)?;
        crate::bedrock::codec::VarInt(self.property_index).encode(buf)?;
        self.type_.encode(buf)?;
        if let Some(v) = &self.value {
            match v {
                PacketPlayerUpdateEntityOverridesValue::SetFloat(v) => {
                    crate::bedrock::codec::F32LE(*v).encode(buf)?;
                }
                PacketPlayerUpdateEntityOverridesValue::SetInt(v) => {
                    crate::bedrock::codec::I32LE(*v).encode(buf)?;
                }
            }
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let runtime_id = <crate::bedrock::codec::VarLong as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let property_index = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let type_ = <PacketPlayerUpdateEntityOverridesType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let value = match type_ {
            PacketPlayerUpdateEntityOverridesType::SetFloat => {
                Some(
                    PacketPlayerUpdateEntityOverridesValue::SetFloat(
                        <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0,
                    ),
                )
            }
            PacketPlayerUpdateEntityOverridesType::SetInt => {
                Some(
                    PacketPlayerUpdateEntityOverridesValue::SetInt(
                        <crate::bedrock::codec::I32LE as crate::bedrock::codec::BedrockCodec>::decode(
                                buf,
                                (),
                            )?
                            .0,
                    ),
                )
            }
            _ => None,
        };
        Ok(Self {
            runtime_id,
            property_index,
            type_,
            value,
        })
    }
}
