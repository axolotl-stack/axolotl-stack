// Generated by valentine_gen. Do not edit.

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unused_parens)]
#![allow(clippy::all)]
use ::bitflags::bitflags;
use bytes::{Buf, BufMut};
use super::*;
use super::super::types::*;
use crate::bedrock::codec::BedrockCodec;
pub use crate::bedrock::protocol::v1_16_201::PacketCamera as PacketCamera;
pub use crate::bedrock::protocol::v1_16_210::PacketCameraShake as PacketCameraShake;
pub use crate::bedrock::protocol::v1_16_210::PacketCameraShakeAction as PacketCameraShakeAction;
#[derive(Debug, Clone, PartialEq)]
pub struct PacketCameraPresets {
    pub presets: Vec<CameraPresets>,
}
impl crate::bedrock::codec::BedrockCodec for PacketCameraPresets {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.presets.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        for item in &self.presets {
            item.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let presets = {
            let raw = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0 as i64;
            if raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "array length cannot be negative",
                    ),
                );
            }
            let len = raw as usize;
            let mut tmp_vec = Vec::with_capacity(len);
            for _ in 0..len {
                tmp_vec
                    .push(
                        <CameraPresets as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        Ok(Self { presets })
    }
}
pub use crate::bedrock::protocol::v1_21_42::PacketCameraInstruction as PacketCameraInstruction;
pub use crate::bedrock::protocol::v1_21_42::PacketCameraInstructionFade as PacketCameraInstructionFade;
pub use crate::bedrock::protocol::v1_21_42::PacketCameraInstructionInstructionSet as PacketCameraInstructionInstructionSet;
pub use crate::bedrock::protocol::v1_21_42::PacketCameraInstructionInstructionSetEaseData as PacketCameraInstructionInstructionSetEaseData;
pub use crate::bedrock::protocol::v1_21_42::PacketCameraInstructionInstructionSetEaseDataType as PacketCameraInstructionInstructionSetEaseDataType;
pub use crate::bedrock::protocol::v1_21_42::PacketCameraInstructionTarget as PacketCameraInstructionTarget;
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum PacketCameraAimAssistTargetMode {
    Angle = 0,
    Distance = 1,
}
impl crate::bedrock::codec::BedrockCodec for PacketCameraAimAssistTargetMode {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as u8;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketCameraAimAssistTargetMode::Angle),
            1 => Ok(PacketCameraAimAssistTargetMode::Distance),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketCameraAimAssistTargetMode), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum PacketCameraAimAssistAction {
    Set = 0,
    Clear = 1,
}
impl crate::bedrock::codec::BedrockCodec for PacketCameraAimAssistAction {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as u8;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketCameraAimAssistAction::Set),
            1 => Ok(PacketCameraAimAssistAction::Clear),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketCameraAimAssistAction), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketCameraAimAssist {
    pub preset_id: String,
    pub view_angle: Vec2F,
    pub distance: f32,
    pub target_mode: PacketCameraAimAssistTargetMode,
    pub action: PacketCameraAimAssistAction,
}
impl crate::bedrock::codec::BedrockCodec for PacketCameraAimAssist {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let bytes: Vec<u8> = if "".eq_ignore_ascii_case("latin1") {
            (&self.preset_id)
                .chars()
                .map(|ch| {
                    let code = ch as u32;
                    if code <= 0xFF { code as u8 } else { b'?' }
                })
                .collect()
        } else {
            (&self.preset_id).as_bytes().to_vec()
        };
        let len = bytes.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        buf.put_slice(&bytes);
        self.view_angle.encode(buf)?;
        crate::bedrock::codec::F32LE(self.distance).encode(buf)?;
        self.target_mode.encode(buf)?;
        self.action.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let preset_id = {
            let len_raw = (<crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as i64;
            if len_raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "string length cannot be negative",
                    ),
                );
            }
            let len = len_raw as usize;
            if buf.remaining() < len {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::UnexpectedEof,
                        format!(
                            "string declared length {} exceeds remaining {}", len, buf
                            .remaining()
                        ),
                    ),
                );
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            let s = if "".eq_ignore_ascii_case("latin1") {
                bytes.into_iter().map(|b| b as char).collect::<String>()
            } else {
                String::from_utf8_lossy(&bytes).into_owned()
            };
            s
        };
        let view_angle = <Vec2F as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let distance = <crate::bedrock::codec::F32LE as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let target_mode = <PacketCameraAimAssistTargetMode as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let action = <PacketCameraAimAssistAction as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            preset_id,
            view_angle,
            distance,
            target_mode,
            action,
        })
    }
}
pub use crate::bedrock::protocol::v1_21_50::PacketCameraAimAssistPresetsCategoriesItem as PacketCameraAimAssistPresetsCategoriesItem;
pub use crate::bedrock::protocol::v1_21_50::PacketCameraAimAssistPresetsCategoriesItemEntityPrioritiesItem as PacketCameraAimAssistPresetsCategoriesItemEntityPrioritiesItem;
pub use crate::bedrock::protocol::v1_21_50::PacketCameraAimAssistPresetsPresetsItem as PacketCameraAimAssistPresetsPresetsItem;
pub use crate::bedrock::protocol::v1_21_50::PacketCameraAimAssistPresetsPresetsItemItemSettingsItem as PacketCameraAimAssistPresetsPresetsItemItemSettingsItem;
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum PacketCameraAimAssistPresetsOperation {
    Set = 0,
    AddToExisting = 1,
}
impl crate::bedrock::codec::BedrockCodec for PacketCameraAimAssistPresetsOperation {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as u8;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketCameraAimAssistPresetsOperation::Set),
            1 => Ok(PacketCameraAimAssistPresetsOperation::AddToExisting),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketCameraAimAssistPresetsOperation), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketCameraAimAssistPresets {
    pub categories: Vec<PacketCameraAimAssistPresetsCategoriesItem>,
    pub presets: Vec<PacketCameraAimAssistPresetsPresetsItem>,
    pub operation: PacketCameraAimAssistPresetsOperation,
}
impl crate::bedrock::codec::BedrockCodec for PacketCameraAimAssistPresets {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.categories.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        for item in &self.categories {
            item.encode(buf)?;
        }
        let len = self.presets.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        for item in &self.presets {
            item.encode(buf)?;
        }
        self.operation.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let categories = {
            let raw = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0 as i64;
            if raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "array length cannot be negative",
                    ),
                );
            }
            let len = raw as usize;
            let mut tmp_vec = Vec::with_capacity(len);
            for _ in 0..len {
                tmp_vec
                    .push(
                        <PacketCameraAimAssistPresetsCategoriesItem as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let presets = {
            let raw = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0 as i64;
            if raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "array length cannot be negative",
                    ),
                );
            }
            let len = raw as usize;
            let mut tmp_vec = Vec::with_capacity(len);
            for _ in 0..len {
                tmp_vec
                    .push(
                        <PacketCameraAimAssistPresetsPresetsItem as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let operation = <PacketCameraAimAssistPresetsOperation as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            categories,
            presets,
            operation,
        })
    }
}
