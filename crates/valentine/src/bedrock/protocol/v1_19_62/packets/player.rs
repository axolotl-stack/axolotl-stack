// Generated by valentine_gen. Do not edit.

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unused_parens)]
#![allow(clippy::all)]
use ::bitflags::bitflags;
use bytes::{Buf, BufMut};
use super::*;
use super::super::types::*;
use crate::bedrock::codec::BedrockCodec;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAction as PacketPlayerAction;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerHotbar as PacketPlayerHotbar;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerInput as PacketPlayerInput;
#[derive(Debug, Clone, PartialEq)]
pub struct PacketPlayerList {
    pub records: PlayerRecords,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerList {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.records.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let records = <PlayerRecords as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self { records })
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketPlayerSkin {
    pub uuid: uuid::Uuid,
    pub skin: Skin,
    pub skin_name: String,
    pub old_skin_name: String,
    pub is_verified: bool,
}
impl crate::bedrock::codec::BedrockCodec for PacketPlayerSkin {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        self.uuid.encode(buf)?;
        self.skin.encode(buf)?;
        let bytes: Vec<u8> = if "".eq_ignore_ascii_case("latin1") {
            (&self.skin_name)
                .chars()
                .map(|ch| {
                    let code = ch as u32;
                    if code <= 0xFF { code as u8 } else { b'?' }
                })
                .collect()
        } else {
            (&self.skin_name).as_bytes().to_vec()
        };
        let len = bytes.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        buf.put_slice(&bytes);
        let bytes: Vec<u8> = if "".eq_ignore_ascii_case("latin1") {
            (&self.old_skin_name)
                .chars()
                .map(|ch| {
                    let code = ch as u32;
                    if code <= 0xFF { code as u8 } else { b'?' }
                })
                .collect()
        } else {
            (&self.old_skin_name).as_bytes().to_vec()
        };
        let len = bytes.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        buf.put_slice(&bytes);
        self.is_verified.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let uuid = <uuid::Uuid as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let skin = <Skin as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        let skin_name = {
            let len_raw = (<crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as i64;
            if len_raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "string length cannot be negative",
                    ),
                );
            }
            let len = len_raw as usize;
            if buf.remaining() < len {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::UnexpectedEof,
                        format!(
                            "string declared length {} exceeds remaining {}", len, buf
                            .remaining()
                        ),
                    ),
                );
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            let s = if "".eq_ignore_ascii_case("latin1") {
                bytes.into_iter().map(|b| b as char).collect::<String>()
            } else {
                String::from_utf8_lossy(&bytes).into_owned()
            };
            s
        };
        let old_skin_name = {
            let len_raw = (<crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0) as i64;
            if len_raw < 0 {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        "string length cannot be negative",
                    ),
                );
            }
            let len = len_raw as usize;
            if buf.remaining() < len {
                return Err(
                    std::io::Error::new(
                        std::io::ErrorKind::UnexpectedEof,
                        format!(
                            "string declared length {} exceeds remaining {}", len, buf
                            .remaining()
                        ),
                    ),
                );
            }
            let mut bytes = vec![0u8; len];
            buf.copy_to_slice(&mut bytes);
            let s = if "".eq_ignore_ascii_case("latin1") {
                bytes.into_iter().map(|b| b as char).collect::<String>()
            } else {
                String::from_utf8_lossy(&bytes).into_owned()
            };
            s
        };
        let is_verified = <bool as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        Ok(Self {
            uuid,
            skin,
            skin_name,
            old_skin_name,
            is_verified,
        })
    }
}
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInput as PacketPlayerAuthInput;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputArgs as PacketPlayerAuthInputArgs;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputBlockActionItem as PacketPlayerAuthInputBlockActionItem;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputBlockActionItemContent as PacketPlayerAuthInputBlockActionItemContent;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputBlockActionItemContentAbortBreak as PacketPlayerAuthInputBlockActionItemContentAbortBreak;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputInputMode as PacketPlayerAuthInputInputMode;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputInteractionModel as PacketPlayerAuthInputInteractionModel;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputPlayMode as PacketPlayerAuthInputPlayMode;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputTransaction as PacketPlayerAuthInputTransaction;
pub use crate::bedrock::protocol::v1_19_60::PacketPlayerAuthInputTransactionArgs as PacketPlayerAuthInputTransactionArgs;
pub use crate::bedrock::protocol::v1_16_220::PacketPlayerEnchantOptions as PacketPlayerEnchantOptions;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerArmorDamage as PacketPlayerArmorDamage;
pub use crate::bedrock::protocol::v1_16_201::PacketPlayerFog as PacketPlayerFog;
