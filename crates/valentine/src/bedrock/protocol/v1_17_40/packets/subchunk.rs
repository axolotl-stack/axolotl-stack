// Generated by valentine_gen. Do not edit.

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unused_parens)]
#![allow(clippy::all)]
use ::bitflags::bitflags;
use bytes::{Buf, BufMut};
use super::*;
use super::super::types::*;
use crate::bedrock::codec::BedrockCodec;
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(i32)]
pub enum PacketSubchunkRequestResult {
    Undefined = 0,
    Success = 1,
    ChunkNotFound = 2,
    InvalidDimension = 3,
    PlayerNotFound = 4,
    YIndexOutOfBounds = 5,
}
impl crate::bedrock::codec::BedrockCodec for PacketSubchunkRequestResult {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as i32;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <i32 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketSubchunkRequestResult::Undefined),
            1 => Ok(PacketSubchunkRequestResult::Success),
            2 => Ok(PacketSubchunkRequestResult::ChunkNotFound),
            3 => Ok(PacketSubchunkRequestResult::InvalidDimension),
            4 => Ok(PacketSubchunkRequestResult::PlayerNotFound),
            5 => Ok(PacketSubchunkRequestResult::YIndexOutOfBounds),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketSubchunkRequestResult), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
#[repr(u8)]
pub enum PacketSubchunkHeightmapType {
    NoData = 0,
    HasData = 1,
    TooHigh = 2,
    TooLow = 3,
}
impl crate::bedrock::codec::BedrockCodec for PacketSubchunkHeightmapType {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let val = *self as u8;
        val.encode(buf)
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let val = <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        match val {
            0 => Ok(PacketSubchunkHeightmapType::NoData),
            1 => Ok(PacketSubchunkHeightmapType::HasData),
            2 => Ok(PacketSubchunkHeightmapType::TooHigh),
            3 => Ok(PacketSubchunkHeightmapType::TooLow),
            _ => {
                Err(
                    std::io::Error::new(
                        std::io::ErrorKind::InvalidData,
                        format!(
                            "Invalid enum value for {}: {}",
                            stringify!(PacketSubchunkHeightmapType), val
                        ),
                    ),
                )
            }
        }
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketSubchunk {
    pub dimension: i32,
    pub x: i32,
    pub y: i32,
    pub z: i32,
    pub data: ByteArray,
    pub request_result: PacketSubchunkRequestResult,
    pub heightmap_type: PacketSubchunkHeightmapType,
    pub heightmap: Option<Vec<u8>>,
}
impl crate::bedrock::codec::BedrockCodec for PacketSubchunk {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.dimension).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.x).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.y).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.z).encode(buf)?;
        let len = self.data.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        for item in &self.data {
            (*item).encode(buf)?;
        }
        self.request_result.encode(buf)?;
        self.heightmap_type.encode(buf)?;
        if let Some(v) = &self.heightmap {
            v.encode(buf)?;
        }
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let dimension = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let x = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let y = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let z = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let data = {
            let res: ByteArray = {
                let raw = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                        buf,
                        (),
                    )?
                    .0 as i64;
                if raw < 0 {
                    return Err(
                        std::io::Error::new(
                            std::io::ErrorKind::InvalidData,
                            "array length cannot be negative",
                        ),
                    );
                }
                let len = raw as usize;
                let mut tmp_vec = Vec::with_capacity(len);
                for _ in 0..len {
                    tmp_vec
                        .push(
                            <u8 as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?,
                        );
                }
                tmp_vec
            };
            res
        };
        let request_result = <PacketSubchunkRequestResult as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let heightmap_type = <PacketSubchunkHeightmapType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let heightmap = match heightmap_type {
            PacketSubchunkHeightmapType::HasData => {
                Some({
                    let raw = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?
                        .0 as i64;
                    if raw < 0 {
                        return Err(
                            std::io::Error::new(
                                std::io::ErrorKind::InvalidData,
                                "array length cannot be negative",
                            ),
                        );
                    }
                    let len = raw as usize;
                    let mut tmp_vec = Vec::with_capacity(len);
                    for _ in 0..len {
                        tmp_vec
                            .push(
                                <u8 as crate::bedrock::codec::BedrockCodec>::decode(
                                    buf,
                                    (),
                                )?,
                            );
                    }
                    tmp_vec
                })
            }
            _ => None,
        };
        Ok(Self {
            dimension,
            x,
            y,
            z,
            data,
            request_result,
            heightmap_type,
            heightmap,
        })
    }
}
#[derive(Debug, Clone, PartialEq)]
pub struct PacketSubchunkRequest {
    pub dimension: i32,
    pub x: i32,
    pub y: i32,
    pub z: i32,
}
impl crate::bedrock::codec::BedrockCodec for PacketSubchunkRequest {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        crate::bedrock::codec::ZigZag32(self.dimension).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.x).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.y).encode(buf)?;
        crate::bedrock::codec::ZigZag32(self.z).encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let dimension = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let x = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let y = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        let z = <crate::bedrock::codec::ZigZag32 as crate::bedrock::codec::BedrockCodec>::decode(
                buf,
                (),
            )?
            .0;
        Ok(Self { dimension, x, y, z })
    }
}
