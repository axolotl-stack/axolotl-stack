// Generated by valentine_gen. Do not edit.

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(dead_code)]
use ::bitflags::bitflags;
use super::*;
use super::super::types::*;
use crate::bedrock::codec::BedrockCodec;
#[derive(Debug, Clone, PartialEq)]
pub struct PacketGraphicsOverrideParameter {
    pub values: Vec<ParameterKeyframeValue>,
    pub biome_identifier: String,
    pub parameter_type: GraphicsOverrideParameterType,
    pub reset: bool,
}
impl crate::bedrock::codec::BedrockCodec for PacketGraphicsOverrideParameter {
    type Args = ();
    fn encode<B: bytes::BufMut>(&self, buf: &mut B) -> Result<(), std::io::Error> {
        let _ = buf;
        let len = self.values.len();
        crate::bedrock::codec::VarInt(len as i32).encode(buf)?;
        for item in &self.values {
            item.encode(buf)?;
        }
        self.biome_identifier.encode(buf)?;
        self.parameter_type.encode(buf)?;
        self.reset.encode(buf)?;
        Ok(())
    }
    fn decode<B: bytes::Buf>(
        buf: &mut B,
        _args: Self::Args,
    ) -> Result<Self, std::io::Error> {
        let _ = buf;
        let values = {
            let len = <crate::bedrock::codec::VarInt as crate::bedrock::codec::BedrockCodec>::decode(
                    buf,
                    (),
                )?
                .0 as usize;
            let mut tmp_vec = Vec::with_capacity(len);
            for _ in 0..len {
                tmp_vec
                    .push(
                        <ParameterKeyframeValue as crate::bedrock::codec::BedrockCodec>::decode(
                            buf,
                            (),
                        )?,
                    );
            }
            tmp_vec
        };
        let biome_identifier = <String as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let parameter_type = <GraphicsOverrideParameterType as crate::bedrock::codec::BedrockCodec>::decode(
            buf,
            (),
        )?;
        let reset = <bool as crate::bedrock::codec::BedrockCodec>::decode(buf, ())?;
        Ok(Self {
            values,
            biome_identifier,
            parameter_type,
            reset,
        })
    }
}
